import React from "react";

// Did this because, for some reason, CircularProgress file did not compile properly.
// Totally lost on why. The code is exactly the same as in the original package.
// In any case, this package is going to go away soon, so not spending more time on it.
export const CircularProgress = () => {
    return <div>🥺</div>;
};
